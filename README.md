# Local Service Radio — landing

Static "on air soon" page. No build step. Deploys to Vercel as-is.

## Local preview

```
open index.html
```

## Deploy

Connect the repo on [vercel.com/new](https://vercel.com/new) and keep all defaults — Vercel auto-detects it as a static site and serves `index.html`.
